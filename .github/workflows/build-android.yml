name: Build TRUTH Android
on: {workflow_dispatch: {}}
jobs: {build: {runs-on: ubuntu-latest, steps: [{uses: 'actions/checkout@v4'}, {uses: 'actions/setup-node@v4', with: {node-version: '20', cache: 'npm'}}, {run: 'npm install'}, {run: 'npx expo prebuild --platform android --clean'}, {run: 'chmod +x android/gradlew'}, {run: 'cd android && ./gradlew assembleDebug'}, {run: 'cp android/app/build/outputs/apk/debug/app-debug.apk android/TRUTH.apk'}, {uses: 'actions/upload-artifact@v4', with: {name: 'TRUTH-APK', path: 'android/TRUTH.apk'}}]}}
